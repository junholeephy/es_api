# 실행 전에 준비할 것

받은 코드만으로는 돌지 않는다. 이 환경에서만 알 수 있는 값과, 이 환경에만 두어야
하는 파일이 남는다. 셋 다 만들고 나서 아래 첫 실행 순서로 간다. 프로그램이 어떻게 도는지는
[`docs/usage.md`](docs/usage.md) 에 있다.

만드는 방법은 `todo/` 에 있다. 여기서는 무엇이 남았는지만 적는다.

| | 무엇 | 왜 여기서 만드나 | 만드는 법 |
|---|---|---|---|
| 1 | `configs/env.yaml` | 접속 정보와 API Key 는 코드와 함께 올 수 없다 | [todo/config.md](todo/config.md) |
| 2 | 실행 스크립트 (`run_daily.sh`) | 매일 도는 명령에서 인자를 빠뜨리면 프로그램은 기본값으로 돌아버린다. 그건 실패가 아니라 **다른 결과**로 나타난다 | [todo/runner.md](todo/runner.md) |
| 3 | 작업 폴더의 `.gitignore` | 받아온 문서와 CSV 가 그대로 커밋될 수 있다 | [todo/gitignore.md](todo/gitignore.md) |

## 이 프로젝트의 사정 / 어느 프로젝트나 해당하는 것

구분해 두는 이유는, 다음에 비슷한 것을 만들 때 **안 해도 될 일을 하거나 반대로
이 프로젝트에만 있는 것을 빠뜨리지 않기 위해서**다.

| 항목 | 구분 |
|---|---|
| `configs/env.yaml` 자체 | 어느 프로젝트나 |
| 그 안의 `elasticsearch.hosts` · `api_key` | **이 프로젝트의 사정** |
| 그 안의 `query.index` · `time_field` · `output.columns` | **이 프로젝트의 사정** |
| `paths.venv` | 어느 프로젝트나 |
| 실행 스크립트 | 어느 프로젝트나 |
| 스케줄 등록 (매일 19:00) | **이 프로젝트의 사정** |
| 작업 폴더의 `.gitignore` | 어느 프로젝트나 |
| `*.jsonl` · `*.csv` 를 막는 줄 | **이 프로젝트의 사정** |

## 첫 실행 순서

```bash
# 1. 접속하지 않고 전 구간이 도는지 본다. 여기서 실패하면 환경 문제다.
python <코드폴더>/src/run.py --dry-run

# 2. 실제 클러스터로 하루치. 화면의 schema 줄이 첫 수확이다.
python <코드폴더>/src/run.py --config configs/env.yaml

# 3. 이후로는 스크립트로. 손으로 치면 인자를 빠뜨린다.
./run_daily.sh
```

2번에서 나오는 `schema` 줄은 **그대로 옮겨 적어 전달한다.** 문서의 실제 형태에 대해
회수할 수 있는 정보가 그것뿐이다. 값 자체는 적지 않는다 — 필드 이름과 건수만이다.
