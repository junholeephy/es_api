################################################################################
# 운영 의존성. 반드시 버전을 고정한다 — 어디서 돌리든 같은 버전을 쓰기 위한 잠금 파일.
# 개발 전용 패키지는 requirements-dev.txt 로.
################################################################################

# Elasticsearch 8.15.3 서버 접속. 클라이언트는 동일 메이저 안에서 호환되므로
# 8.x 최신을 쓴다 — 서버 마이너에 맞춘 8.15.1 보다 버그 수정이 앞선다.
elasticsearch==8.19.3

# configs/env.yaml 파싱. run.py 는 이것을 쓰지 않는다 (venv 전환 전에 돌기 때문).
PyYAML==6.0.3
